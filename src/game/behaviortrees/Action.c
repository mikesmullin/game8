#include "Action.h"

void Action__PerformBuffered(Entity* entity, OnActionParams* action) {
  // if (NULL != entity->sg) {
  //   if (NULL != entity->sg->actions) {
  //     entity->sg->actions(entity->sg, action);
  //   }
  // }
}